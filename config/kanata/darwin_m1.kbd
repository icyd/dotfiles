(defsrc
  grv     1       2       3       4       5       6       7       8       9       0       -       =       bspc
  tab     q       w       e       r       t       y       u       i       o       p       [       ]       \
  caps    a       s       d       f       g       h       j       k       l       ;       '
  lsft    lsgt    z       x       c       v       b       n       m       ,       .       /       rsft
  fn      lctl    lalt    lmet                    spc                     rmet    ralt
)

(defvar
  tap-time 200
  hold-time 200
  cw-timeout 3000
)

(defalias
  fn (multi fn (layer-while-held function))
  qwt (layer-switch qwerty)
  eth (layer-switch enthium)
  func (layer-switch function)
  cw (caps-word $cw-timeout)
  num (layer-switch numbers)
  sym (layer-switch symbols)
  nav (layer-switch navigation)
  alt_y (tap-hold-release $tap-time $hold-time y ralt)
  alt_c (tap-hold-release $tap-time $hold-time c lalt)
  met_i (tap-hold-release $tap-time $hold-time i lmet)
  ctl_a (tap-hold-release $tap-time $hold-time a lctl)
  sft_e (tap-hold-release $tap-time $hold-time e lsft)
  alt_p (tap-hold-release $tap-time $hold-time p ralt)
  sft_h (tap-hold-release $tap-time $hold-time h rsft)
  ctl_t (tap-hold-release $tap-time $hold-time t lctl)
  met_n (tap-hold-release $tap-time $hold-time n lmet)
  alt_s (tap-hold-release $tap-time $hold-time s ralt)
  nm_bks (tap-hold-press $tap-time $hold-time bspc (layer-toggle numbers))
  sym_r (tap-hold-release $tap-time $hold-time r (layer-toggle symbols))
  nav_' (tap-hold-release $tap-time $hold-time ' (layer-toggle navigation))
  copy M-c
  cut M-x
  find M-f
  paste M-v
  redo M-y
  sall M-a
  undo M-z
  dcln (macro S-; S-;)
)

(deflayer enthium
  lsgt    1       2       3       4       5       6       7       8       9       0       -       =       bspc
  _       q       @alt_y  o       u       =       x       l       d       @alt_p  z       esc     ]       \
  b       @alt_c  @met_i  @ctl_a  @sft_e  -       k       @sft_h  @ctl_t  @met_n  @alt_s  w
  _       grv     @nav_'  ,       .       ;       /       j       m       g       f       v       _
  @fn     _       _       @nm_bks                 _                       @sym_r  _
)

(deflayer navigation
  XX      f1      f2      f3      f4      f5      f6      f7      f8      f9      f10     f11     f12     del
  @func   XX      XX      XX      @undo   @redo   XX      home    pgup    pgdn    end     @eth    XX      XX
  XX      XX      XX      @sall   @find   XX      XX      left    up      down    right   XX
  @cw     XX      XX      @cut    @copy   @paste  XX      XX      XX      XX      XX      @nav    @cw
  @fn     XX      XX      bspc                    tab                     del     _
)

(deflayer numbers
  XX      XX      XX      XX      XX      XX      XX      XX      XX      XX      XX      XX      XX      bspc
  @num    S-1     S-4     S-0     S-7     =       S-5     S-6     S-3     S-8     S-4     @eth    XX      XX
  XX      6       4       0       2       S--     k       3       1       5       7       XX
  lsft    XX      XX      XX      8       S-;     /       9       XX      S-g     XX      @num    rsft
  @fn     XX      XX      bspc                    spc                     bspc    _
)

(deflayer symbols
  _       S-1     S-2     S-3     S-4     S-5     S-6     S-7     S-8     S-9     S-0     S--     S-=     _
  @sym    S-1     S-[     S-]     S-7     S-=     S-5     S-6     S-3     S-8     S-4     @eth    _       _
  XX      grv     S-9     S-0     \       S--     S-grv   S-\     [       ]       XX      XX
  _       _       S-'     S-,     S-.     S-;     S-/     S-2     @dcln   XX      XX      @sym    _
  @fn     _       _       bspc                    _                       r       _
)

(deflayer qwerty
  grv     1       2       3       4       5       6       7       8       9       0       -       =       _
  _       q       w       e       r       t       y       u       i       o       p       [       ]       \
  caps    a       s       d       f       g       h       j       k       l       ;       '
  _       lsgt    z       x       c       v       b       n       m       ,       .       /       _
  @fn     _       _       lmet                    _                       rmet    _
)

(deflayer function
  XX      f1      f2      f3      f4      f5      f6      f7      f8      f9      f10     f11     f12     _
  @qwt    XX      XX      XX      XX      XX      XX      XX      XX      XX      XX      @eth    XX      XX
  f12     f6      f4      f10     f2      XX      XX      f3      f1      f5      f7      f11
  XX      XX      XX      -       f8      XX      XX      f11     XX      XX      XX      XX      XX
  @fn     XX      XX      @num                    XX                      @sym    _
)
